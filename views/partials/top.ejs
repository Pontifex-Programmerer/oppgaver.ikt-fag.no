<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="main.css">
    <% if(typeof title === 'undefined') { %>
        <title>ikt-fag.no</title>
    <% } else { %>
        <title><%=title%></title>
    <% } %>
</head>
<body>