<section class="error-message">
    <% if(typeof(errormessage)!=='undefined') {%>
    <p><%= errormessage %></p>
    <% } else { %>
        <p>This is awkward, but it seems there was an error displaying an errormessage...</p>
    <% } %>
</section>